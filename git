git add -A
git add .
git status
git commit -m "Message"
git push
git push -u origin master



master






SWITCH TO ANOTHER EXISTING BRANCH

ALWAYS!
git status
git add .
git commit -m "message"

THEN
git checkout production
git checkout staging
git checkout feature/contact-page
git checkout feature/services-page
git checkout hotfix/form-bug



CREATING A NEW FEATURE BRANCH

git checkout production
git checkout -b feature/richards-branch

... make changes
git add .
git commit -m "message"
git push -u origin feature/richards-branch

... make more changes
git add .
git commit -m "message"
git push

... make more changes
git add .
git commit -m "message"
git push



UPDATING BRANCHES

git checkout feature/richard
git pull



UPDATING FROM ANOTHER BRANCH

git checkout feature/richard
git pull origin production



PUT FEATURE EXPERIMENT ONTO STAGING

git add .
git commit -m "message"

git checkout staging
git git pull origin feature/richard
(git commit)
git push




git branch
